# TODO

## FastDebugger()

- Should print a list like `ic()`

## README.md

- Write all available functions to a nice list
- Add function examples
    - `chunker()`
    - `join_path()`
    - `read_json()`
    - `write_json()`
    - `create_json()`
    - `FastDebugger()`